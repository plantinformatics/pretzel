;; emacs (text editor) configuration.
;; this file applies to files in sub-directories.
(
 ;; For js-mode (Javascript) buffers, display tabs as 2 columns and
 ;; auto-indent by 2 columns.
 (js-mode
  . ((tab-width . 2)
     (js-indent-level . 2)
     ))
 )
