<div class="btn-group">
  <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
    Choose mapset
    <span class="caret"></span>
  </button>
  <ul class="dropdown-menu" role="menu">
  {{#each availableMaps as |mymap|}}
  <li class="{{if mymap.isSelected "disabled" "enabled"}}">
    {{#link-to "mapview" (query-params mapsToView=mymap.mapLink)}}
      {{mymap.name}}
    {{/link-to}}
  </li>
  {{/each}}
  </ul>
</div>

<br/>
<br/>

Maps to align:

<ul>
{{#each model as |mapset|}}
<li>{{mapset.name}}, {{mapset.class}}, {{mapset.isPub}}</li>
  <!--<ul>
    {{#each mapset.maps as |m|}}
      <li>{{m.name}}</li>
      <ul>
        {{#each m.markermaplocations as |marker|}}
          <li>{{marker.location}}, {{marker.marker.commonName}}</li>
        {{/each}}
      </ul>
    {{/each}}
  </ul>-->
  <!-- call draw-map component here. the mapset or other objects can be passed into the component. -->
  {{draw-map selectedMapset=mapset}} 
{{/each}}
</ul>
<!--{{draw-map selectedMapset=mapset}}-->
<!--{{draw-map mapset=model map=mapset.maps}}-->

{{outlet}}
