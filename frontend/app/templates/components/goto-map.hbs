<ul class="list-group">
  {{#each this.mapList as |map|}}
    {{record/entry-dataset
      entry=map
      selectedBlock=this.selectedBlock
      selectBlock=(action this.actions.selectBlock)
      deleteBlock=(action this.actions.deleteBlock)}}
  {{/each}}
</ul>
