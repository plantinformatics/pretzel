<ul class="list-group">
  {{#each mapList as |map|}}
    {{record/entry-dataset
      entry=map
      selectedBlock=selectedBlock
      selectBlock="selectBlock"
      deleteBlock="deleteBlock"}}
  {{/each}}
</ul>
