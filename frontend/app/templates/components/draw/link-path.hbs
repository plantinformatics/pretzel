{{!--
display summary information about paths;
these (summary) keys are suited to display adjustment : e.g.
* threshold slider;
* select which references to show aliases via, and how to colour/style them.
--}}
