{{!-- stacksCount : {{ stacksCount } }, {{ axesP.length --}}

{{this.axesViewedEffect.length}}
{{log 'stacks-view' 'axesP' this.stacks.length this.stacks this }}
{{this.stacks.length}}
{{#each this.axesViewedEffect as | stack | }}
  {{stack.portions.length}}
{{/each}}

{{draw/axes-1d drawMap=this.drawMap axes2d=this.axes2d model=this.model stacksView=this featuresInBlocks=this.featuresInBlocks
}}
{{#if @model.userSettings.genotype.showTablePositionAlignment}}
{{draw/graph-annotations
  model=this.model
  stacksView=this
  rightAxes=this.rightStack.axes
}}
{{/if}}
{{yield}}