<ul class="list-group borderless">
  {{#each this.data as |dataset|}}

    {{record/entry-dataset-level
      name=dataset.displayName
      values=dataset
      loadBlock="loadBlock"
      selectBlock=this.selectBlock
      selectedBlock=this.selectedBlock
      selectDataset=this.selectDataset}}

  {{/each}}
</ul>
