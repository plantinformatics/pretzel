{{#if entry.isViewed}}
{{else}}
  {{elem/button-base
    classSize='xs'
    classColour='success'
    icon='plus'
    onClick=(action "loadBlock" entry)}}
{{/if}}
{{entry.datasetId.content.name}}
{{entry.name}}
{{entry.scope}}
{{entry.namespace}}
<span class="badge" style="background-color: #999;">
  {{entry.count}}
</span>
<!-- {{entry.id}}  -->
