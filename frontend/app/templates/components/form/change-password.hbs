{{#form/auth-base title="Change Your Password" errorMessage=errorMessage}}
  {{#if isRegistered}}
    {{#elem/panel-form
      name="success"
      heading="Success"}}
      Your password has been changed.
    {{/elem/panel-form}}
  {{else}}
    <form {{action 'submitForm' on='submit'}}>
      {{auth-input
        type='password'
        value=oldPassword
        placeholder='Old Password'}}
      {{auth-input
        type='password'
        value=newPassword
        placeholder='New Password'}}
      {{suggest-link
        suggest-text="Can't remember your password?"
        link-name="reset-request"
        link-pretty="Reset"}}
      {{submit-button loading=isProcessing}}
    </form>
  {{/if}}
{{/form/auth-base}}