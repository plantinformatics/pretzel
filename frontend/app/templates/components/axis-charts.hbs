{{#if blocks}}
  {{#each blocks as |chartBlock|}}
    {{draw/block-view axis=axis axisID=axisID block=chartBlock blocksData=blocksData}}
  {{/each}}
  {{log 'chartTypesEffect' chartTypesEffect }}
{{else}}
<div class="Chart">
  {{content-editable
    value=selection
    placeholder="Paste Chart Here"
    class="chart pasteData"
    type="text"}}

</div>
{{/if}}
