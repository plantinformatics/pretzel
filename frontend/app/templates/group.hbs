{{#let this.model as |group|}}
  <label>Group</label>
  <div>

     <div class="margin-1em" style="display:flex; gap:1em">
      {{group.id}}
      {{group.clientId}}
      {{group.name}}
     </div>

 </div>


{{/let}}

{{outlet}}